#include <stdio.h>
#include "main.h"

/**
 * _strlen_recursion - length of string
 * @char * : data type
 * @s : character string
 * Return : integer value of string length
 */
int _strlen_recursion(char *s)
{
	int strlen = 0;

	if(*s == '\0')
	{
		printf("%d", strlen);
		return (0);
	}
	return (0);
}
