#include <stdio.h>
#include "main.h"

/**
 * _strlen_recursion - prints legnth of string
 * @char * : data type
 * @s : string
 * Return : 0
 */
int _strlen_recursion(char *s)
{
	int n;

	n = 1;
	s = 0;

	if (*s == '\0')
	{
		printf("%d\n", n);
		return (0);
	}
		return (0);
}
