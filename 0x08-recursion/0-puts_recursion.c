#include "main.h"
#include <stdio.h>

/**
 * Print puts string
 *
 * Return: Always 0.
 */
void _puts_recursion(char *s)
{
	if (*s)
		return 1;
	else
		return *s  = *s * (*s - 1);
}

int main ()
{
	
printf ("%d\n", *s);
	return 0;
}
