#include "main.h"
#include <stdio.h>

/**
 * Print puts string
 *
 * Return: Always 0.
 */

void _puts_recursion(char *s)
{
	if (*s <= 1)
		return 1;
	else
		return *s  = *s * (*s - 1);
}
	char main ()
	{
		printf ("%d\n", *s(10));
		return 0;
}
