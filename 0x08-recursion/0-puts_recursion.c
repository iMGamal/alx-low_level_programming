#include <main.h>
#include <stdio.h>

/**
 * Print puts string
 *
 * Return: Always 0.
 */

void _puts_recursion(char *s);
{
	//base case
	if (puts == 0)
		return 1;
	else
		return puts = puts * (puts - 1);

	int main ()
{
	printf ("%d\n", puts(10));
	return 0;
}
