#include <stdio.h>
#include "main.h"

/**
 * _isupper - function that returns integer
 * @int : data type
 * @c : parameter
 * Return : 0
 */
int _isupper(int c)
{
	if (c > 0)
	{
		printf("%d", c);
		return (0);
	}
	return (0);
}
